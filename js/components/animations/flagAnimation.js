// Laptop animation
// import { gsap, Linear } from "gsap";

// const flags = document.querySelectorAll(".js-flag-animation");

// if (flags.length > 0) {
// 	Array.from(flags).forEach((flag) => {
// 		gsap.to(flag, { x: 50 });
// 		gsap.to(flag, { x: 0, repeat: -1, ease: Linear.easeNone });
// 	});
// }
