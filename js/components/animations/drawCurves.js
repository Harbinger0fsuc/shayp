// import { gsap } from "gsap";
// import preparePath from "../../functions/preparePath";

// const lines = document.querySelectorAll(".js-curve-animation");

// if (lines.length > 0) {
// 	Array.from(lines).forEach((line) => {
// 		preparePath(line);
// 		// console.log(line);

// 		gsap.to(line, { strokeDashoffset: 0, ease: "none", duration: 1.5, repeat: -1 });
// 	});
// }
